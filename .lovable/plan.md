

# سوپرپرامپت فرانت‌اند چت‌بات نئوهوش + آنبوردینگ

---

## ۱. ساختار کلی صفحه چت (`Chat.tsx`)

صفحه چت یک `div` تمام‌صفحه (`h-screen flex flex-col bg-background`) است که از سه بخش اصلی تشکیل شده:

```text
┌─────────────────────────────────┐
│          Header (sticky)        │
├─────────────────────────────────┤
│                                 │
│       Messages Area             │
│    (flex-1 overflow-y-auto)     │
│    max-w-2xl mx-auto            │
│                                 │
├─────────────────────────────────┤
│     Input Bar (sticky bottom)   │
└─────────────────────────────────┘
```

---

## ۲. هدر (Header)

- **ظاهر:** `border-b border-border/40 bg-card/80 backdrop-blur-md sticky top-0 z-50`
- **عرض محتوا:** `max-w-3xl mx-auto px-4 py-3`
- **چیدمان:** `flex items-center justify-between`
- **سمت راست:** دکمه تاریخچه (آیکون `History`) — فقط برای کاربران لاگین‌شده. دکمه دایره‌ای `h-9 w-9 rounded-full hover:bg-muted/60`
- **وسط:** عنوان برند `"NeoHoosh"` با `absolute left-1/2 -translate-x-1/2`. فونت: `text-xl font-brand font-semibold tracking-tight`
- **سمت چپ:** آواتار کاربر (دایره `h-8 w-8 rounded-full bg-primary/20` با حرف اول ایمیل) یا دکمه "ورود" (`variant="outline" rounded-xl`)

---

## ۳. حالت خالی (Empty State)

وقتی هیچ پیامی نیست:
- انیمیشن ورود: `initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }}`
- عنوان: `"سلام! چطور می‌تونم کمکت کنم؟"` — `text-2xl font-semibold`
- زیرعنوان: `text-muted-foreground text-sm mb-10`
- **شبکه پیشنهادات:** `grid grid-cols-2 gap-3 max-w-lg`
  - هر کارت: `rounded-2xl bg-card/50 border border-border/40 hover:bg-card hover:border-border/80 hover:shadow-sm`
  - ایموجی + عنوان کوتاه + متن پیشنهادی
  - کلیک روی هر کارت → متن در اینپوت قرار می‌گیرد

---

## ۴. پیام‌ها

### پیام کاربر:
- چیدمان: `flex justify-end`
- حباب: `bg-muted/80 text-foreground px-4 py-3 rounded-2xl rounded-br-md`
- عرض حداکثر: `max-w-[85%] sm:max-w-[80%]`
- متن: `text-sm whitespace-pre-wrap leading-relaxed`
- **کپی با نگه‌داشتن:** ۵۰۰ میلی‌ثانیه لانگ‌پرس → دکمه کپی بالای حباب ظاهر می‌شود

### پیام دستیار:
- چیدمان: `flex justify-start`
- **آواتار:** دایره `w-7 h-7 ml-2 mt-1 rounded-full bg-gradient-to-br from-primary/20 to-primary/10` با آیکون `Sparkles` در وسط
- **بدون حباب!** متن مستقیم بدون پس‌زمینه رندر می‌شود (سبک ChatGPT)
- محتوا با `ReactMarkdown` + `remarkGfm` رندر می‌شود
- کلاس‌های `prose`: 
  ```
  prose prose-sm max-w-none leading-relaxed
  [&_*]:text-foreground
  [&_p]:leading-7 [&_p]:mb-2
  [&_h1]:text-lg [&_h1]:font-bold
  [&_h2]:text-base [&_h2]:font-semibold
  [&_blockquote]:border-r-2 [&_blockquote]:border-primary
  ```
- بلوک‌های کد: کامپوننت `CodeBlock` جداگانه
- جداول: `overflow-x-auto rounded-lg border border-border/50`

### آیکون‌های اکشن زیر پاسخ دستیار:
- چیدمان: `flex items-center gap-3 mt-4` — **افقی، فاصله یکسان، بدون لیبل متنی**
- هر دکمه: `p-2 rounded-lg text-muted-foreground hover:text-foreground hover:bg-muted/50`
- آیکون‌ها (به ترتیب): `Copy` → `ThumbsUp` → `ThumbsDown` → `FileText` (خلاصه)
- اندازه آیکون‌ها: `w-4 h-4`
- حالت فعال Like: `text-emerald-500 bg-emerald-500/10 fill-current`
- حالت فعال Dislike: `text-rose-500 bg-rose-500/10 fill-current`
- حالت کپی‌شده: آیکون `Check` سبز جایگزین `Copy` می‌شود

---

## ۵. انیمیشن تایپینگ

وقتی مدل در حال پاسخ‌دهی است و هنوز محتوایی نرسیده:
- **یک** آواتار `Sparkles` چرخان (`rotate: 360`, `duration: 2s`, `repeat: Infinity`, `ease: linear`)
- سه نقطه پالسی کنار آن:
  - هر نقطه: `w-1.5 h-1.5 bg-primary/70 rounded-full`
  - انیمیشن: `scale: [1, 1.3, 1]` و `opacity: [0.5, 1, 0.5]`
  - مدت: `1.2s` تکرار بی‌نهایت
  - تاخیر بین نقاط: `0s`, `0.2s`, `0.4s`

---

## ۶. نوار ورودی (Input Bar)

- کانتینر: `border-t border-border/40 bg-background sticky bottom-0`
- عرض: `max-w-2xl mx-auto px-4 py-4`
- باکس داخلی: `rounded-2xl bg-card border border-border/50 p-2 flex items-end gap-1`
- **دکمه انتخاب مدل:** آیکون `Plus` در سمت راست. `h-10 w-10 rounded-xl`. با کلیک `Popover` باز می‌شود
- **تکست‌اریا:** بدون بوردر (`border-0 bg-transparent focus-visible:ring-0`), `text-[14px]`, `placeholder: "به چی فکر میکنی..."`
- **دکمه ارسال/توقف:** `h-10 w-10 rounded-xl`. ارسال: `bg-primary`. توقف: `bg-destructive` با آیکون `Square`
- **رفتار Enter:** موبایل → فقط خط جدید. دسکتاپ → Enter ارسال، Shift+Enter خط جدید

### Popover انتخاب مدل:
- `w-64 p-2`, باز شدن به بالا (`side="top"`)
- هر مدل: `px-3 py-2.5 rounded-xl` با آیکون در مربع `w-8 h-8 rounded-lg`
- مدل فعال: `bg-primary/10 text-primary` + آیکون `Check`
- ۵ مدل: عمومی، کسب‌وکار، شخصی، تبلیغات، تحصیلی

---

## ۷. پنل تاریخچه (History Sheet)

- `Sheet` از سمت راست: `w-[90vw] sm:w-[420px]`
- هدر: عنوان "تاریخچه گفتگوها" + دکمه بستن
- دکمه "گفتگوی جدید": `rounded-2xl bg-primary/10 hover:bg-primary/20 text-primary`
- هر آیتم: `rounded-xl border` با عنوان، تاریخ (فارسی)، نام مدل
- آیتم فعال: `border-primary/60 bg-primary/5 shadow-sm`
- دکمه حذف: `opacity-0 group-hover:opacity-100` در سمت چپ
- حالت خالی: آیکون بزرگ + متن "هنوز گفتگویی ثبت نشده"

---

## ۸. پروفایل (ProfileDrawer)

- `Sheet` از سمت چپ: `w-[90vw] sm:w-[380px]`
- هدر با دکمه بستن + عنوان "پروفایل"
- آواتار دایره‌ای `w-20 h-20 rounded-full bg-primary/15`
- کارت‌های اطلاعات: `bg-card/30 rounded-2xl border border-border/30 p-4`
  - نام (قابل ویرایش)
  - ایمیل
  - تاریخ عضویت (فارسی)
- آمار: شبکه ۲ ستونه (تعداد پیام + تعداد گفتگو)
- دکمه "تنظیمات AI": با آیکون `Brain` و گرادیانت
- دکمه "نصب اپلیکیشن" (شرطی): `bg-gradient-to-r from-primary/20 to-primary/10 border-primary/30`
- دکمه خروج: `text-destructive/70 hover:text-destructive`
- فوتر: "نئوهوش • نسخه ۱.۰.۰"

---

## ۹. دیالوگ‌ها

### دیالوگ محدودیت مهمان:
- آیکون `Sparkles` در مربع `w-16 h-16 rounded-2xl bg-primary/10` با انیمیشن spring
- عنوان: "برای ادامه وارد شوید ✨"
- دکمه: "ورود / ثبت‌نام" — `h-12 rounded-xl`

### دیالوگ محدودیت Context:
- ایموجی 📝 متحرک در مربع `w-20 h-20 rounded-3xl bg-gradient-to-br from-amber-500/20 to-orange-500/20`
- انیمیشن ایموجی: `y: [0, -5, 0]` تکرار بی‌نهایت
- عنوان: "گفتگو خیلی طولانی شده!"
- دو دکمه: "بله، گفتگوی جدید" (`rounded-2xl bg-gradient-to-r from-primary`) و "فعلاً نه" (`variant="outline" rounded-2xl`)

---

## ۱۰. آنبوردینگ (OnboardingTour)

### ساختار:
- اورلی تمام‌صفحه: `fixed inset-0 z-[100] bg-black/60 backdrop-blur-sm`
- کارت مرکزی: `max-w-md bg-card border border-border/50 rounded-3xl shadow-2xl`
- ذخیره وضعیت در `localStorage` (کلید: `neohoosh_onboarding_completed`) — فقط یک بار نمایش

### ۳ مرحله:
1. **خوش‌آمدگویی:** آیکون `Sparkles` — "به نئوهوش خوش آمدید! 🎉"
2. **چت هوشمند:** آیکون `MessageSquare` — توضیح قابلیت‌های چت
3. **تاریخچه:** آیکون `History` — ذخیره مکالمات

### بخش‌های هر مرحله:
- **هدر گرادیانت:** `h-32 bg-gradient-to-br from-primary/20 via-primary/10 to-transparent` + radial gradient
- **آیکون مرکزی:** `p-4 rounded-2xl bg-primary/10 text-primary` با انیمیشن spring (`scale: 0 → 1`)
- **دکمه بستن (X):** `absolute top-4 left-4 rounded-full hover:bg-white/10`
- **شمارنده مرحله:** `absolute top-4 right-4 text-xs bg-background/50 px-2 py-1 rounded-full`
- **محتوا:** عنوان `text-xl font-bold` + توضیح `text-muted-foreground text-sm leading-relaxed` در `text-center`
- **نقاط پیشرفت:** `gap-1.5` — فعال: `w-6 h-2 bg-primary rounded-full` — غیرفعال: `w-2 h-2 bg-muted-foreground/30`
- **دکمه‌های ناوبری:** `border-t border-border/50 bg-muted/30 p-4 flex justify-between`
  - قبلی: `variant="ghost"` + آیکون `ChevronRight` (RTL)
  - رد شدن: `variant="ghost" text-muted-foreground`
  - بعدی/شروع: `size="sm" min-w-[80px]` + آیکون `ChevronLeft` (RTL). آخرین مرحله: "شروع کنید!"

### انیمیشن‌ها:
- ورود کارت: `scale: 0.9 → 1, y: 20 → 0` با `spring(damping: 25, stiffness: 300)`
- خروج: `scale: 1 → 0.9, y: 0 → -20`
- عنوان و توضیح: `opacity: 0 → 1, y: 10 → 0` (توضیح با `delay: 0.1`)

---

## ۱۱. رنگ‌بندی (از CSS Variables)

| عنصر | لایت | دارک |
|---|---|---|
| `--background` | `0 0% 100%` (سفید) | `240 10% 3.9%` (تقریبا سیاه) |
| `--card` | `0 0% 100%` | `240 10% 3.9%` |
| `--foreground` | `240 10% 3.9%` | `0 0% 98%` |
| `--primary` | `221.2 83.2% 53.3%` (آبی) | `217.2 91.2% 59.8%` |
| `--muted` | `240 4.8% 95.9%` | `240 3.7% 15.9%` |
| `--muted-foreground` | `240 3.8% 46.1%` | `240 5% 64.9%` |
| `--border` | `240 5.9% 90%` | `240 3.7% 15.9%` |
| `--destructive` | `0 84.2% 60.2%` (قرمز) | `0 62.8% 30.6%` |

---

## ۱۲. تایپوگرافی و جهت

- فونت اصلی: `Vazirmatn` (فارسی) + `Inter` (لاتین)
- جهت: `direction: rtl` (تنظیم‌شده در `index.css` روی `*`)
- فونت برند هدر: `font-brand`

---

## ۱۳. انیمیشن‌های عمومی

- ورود پیام‌ها: `opacity: 0 → 1, y: 10 → 0` با `duration: 0.2`
- `AnimatePresence mode="popLayout"` برای مدیریت لیست پیام‌ها
- تمام دیالوگ‌ها با انیمیشن `spring` باز می‌شوند

